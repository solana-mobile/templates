/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithPayer,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type ExtendedClient,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from '@solana/kit'
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from '@solana/kit/program-client-core'
import { getCounterCodec, type Counter, type CounterArgs } from '../accounts'
import {
  getIncrementInstructionAsync,
  getInitializeInstructionAsync,
  parseIncrementInstruction,
  parseInitializeInstruction,
  type IncrementAsyncInput,
  type InitializeAsyncInput,
  type ParsedIncrementInstruction,
  type ParsedInitializeInstruction,
} from '../instructions'
import { findCounterPda, findInitializeCounterPda } from '../pdas'

export const HELLO_WORLD_PROGRAM_ADDRESS =
  'BneHHhN7nSXgjZytQWLsi1TRY7xMz83FxpkKgoCb1n2d' as Address<'BneHHhN7nSXgjZytQWLsi1TRY7xMz83FxpkKgoCb1n2d'>

export enum HelloWorldAccount {
  Counter,
}

export function identifyHelloWorldAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): HelloWorldAccount {
  const data = 'data' in account ? account.data : account
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(new Uint8Array([255, 176, 4, 245, 188, 253, 124, 25])),
      0,
    )
  ) {
    return HelloWorldAccount.Counter
  }
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT, {
    accountData: data,
    programName: 'helloWorld',
  })
}

export enum HelloWorldInstruction {
  Increment,
  Initialize,
}

export function identifyHelloWorldInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): HelloWorldInstruction {
  const data = 'data' in instruction ? instruction.data : instruction
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(new Uint8Array([11, 18, 104, 9, 104, 174, 59, 33])),
      0,
    )
  ) {
    return HelloWorldInstruction.Increment
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(new Uint8Array([175, 175, 109, 31, 13, 152, 155, 237])),
      0,
    )
  ) {
    return HelloWorldInstruction.Initialize
  }
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION, {
    instructionData: data,
    programName: 'helloWorld',
  })
}

export type ParsedHelloWorldInstruction<TProgram extends string = 'BneHHhN7nSXgjZytQWLsi1TRY7xMz83FxpkKgoCb1n2d'> =
  | ({ instructionType: HelloWorldInstruction.Increment } & ParsedIncrementInstruction<TProgram>)
  | ({ instructionType: HelloWorldInstruction.Initialize } & ParsedInitializeInstruction<TProgram>)

export function parseHelloWorldInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedHelloWorldInstruction<TProgram> {
  const instructionType = identifyHelloWorldInstruction(instruction)
  switch (instructionType) {
    case HelloWorldInstruction.Increment: {
      assertIsInstructionWithAccounts(instruction)
      return { instructionType: HelloWorldInstruction.Increment, ...parseIncrementInstruction(instruction) }
    }
    case HelloWorldInstruction.Initialize: {
      assertIsInstructionWithAccounts(instruction)
      return { instructionType: HelloWorldInstruction.Initialize, ...parseInitializeInstruction(instruction) }
    }
    default:
      throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE, {
        instructionType: instructionType as string,
        programName: 'helloWorld',
      })
  }
}

export type HelloWorldPlugin = {
  accounts: HelloWorldPluginAccounts
  instructions: HelloWorldPluginInstructions
  pdas: HelloWorldPluginPdas
  identifyAccount: typeof identifyHelloWorldAccount
  identifyInstruction: typeof identifyHelloWorldInstruction
  parseInstruction: typeof parseHelloWorldInstruction
}

export type HelloWorldPluginAccounts = {
  counter: ReturnType<typeof getCounterCodec> & SelfFetchFunctions<CounterArgs, Counter>
}

export type HelloWorldPluginInstructions = {
  increment: (input: IncrementAsyncInput) => ReturnType<typeof getIncrementInstructionAsync> & SelfPlanAndSendFunctions
  initialize: (
    input: MakeOptional<InitializeAsyncInput, 'payer'>,
  ) => ReturnType<typeof getInitializeInstructionAsync> & SelfPlanAndSendFunctions
}

export type HelloWorldPluginPdas = {
  counter: typeof findCounterPda
  initializeCounter: typeof findInitializeCounterPda
}

export type HelloWorldPluginRequirements = ClientWithRpc<GetAccountInfoApi & GetMultipleAccountsApi> &
  ClientWithPayer &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending

export function helloWorldProgram() {
  return <T extends HelloWorldPluginRequirements>(client: T): ExtendedClient<T, { helloWorld: HelloWorldPlugin }> => {
    return extendClient(client, {
      helloWorld: <HelloWorldPlugin>{
        accounts: { counter: addSelfFetchFunctions(client, getCounterCodec()) },
        instructions: {
          increment: (input) => addSelfPlanAndSendFunctions(client, getIncrementInstructionAsync(input)),
          initialize: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeInstructionAsync({ ...input, payer: input.payer ?? client.payer }),
            ),
        },
        pdas: { counter: findCounterPda, initializeCounter: findInitializeCounterPda },
        identifyAccount: identifyHelloWorldAccount,
        identifyInstruction: identifyHelloWorldInstruction,
        parseInstruction: parseHelloWorldInstruction,
      },
    })
  }
}

type MakeOptional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>
