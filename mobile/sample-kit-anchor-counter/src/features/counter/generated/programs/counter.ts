/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithPayer,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type ExtendedClient,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from '@solana/kit'
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from '@solana/kit/program-client-core'
import { getCounterCodec, type Counter, type CounterArgs } from '../accounts'
import {
  getIncrementInstructionAsync,
  getInitializeInstructionAsync,
  parseIncrementInstruction,
  parseInitializeInstruction,
  type IncrementAsyncInput,
  type InitializeAsyncInput,
  type ParsedIncrementInstruction,
  type ParsedInitializeInstruction,
} from '../instructions'
import { findCounterPda } from '../pdas'

export const COUNTER_PROGRAM_ADDRESS =
  'ADraQ2ENAbVoVZhvH5SPxWPsF2hH5YmFcgx61TafHuwu' as Address<'ADraQ2ENAbVoVZhvH5SPxWPsF2hH5YmFcgx61TafHuwu'>

export enum CounterAccount {
  Counter,
}

export function identifyCounterAccount(account: { data: ReadonlyUint8Array } | ReadonlyUint8Array): CounterAccount {
  const data = 'data' in account ? account.data : account
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(new Uint8Array([255, 176, 4, 245, 188, 253, 124, 25])),
      0,
    )
  ) {
    return CounterAccount.Counter
  }
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT, {
    accountData: data,
    programName: 'counter',
  })
}

export enum CounterInstruction {
  Increment,
  Initialize,
}

export function identifyCounterInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): CounterInstruction {
  const data = 'data' in instruction ? instruction.data : instruction
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(new Uint8Array([11, 18, 104, 9, 104, 174, 59, 33])),
      0,
    )
  ) {
    return CounterInstruction.Increment
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(new Uint8Array([175, 175, 109, 31, 13, 152, 155, 237])),
      0,
    )
  ) {
    return CounterInstruction.Initialize
  }
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION, {
    instructionData: data,
    programName: 'counter',
  })
}

export type ParsedCounterInstruction<TProgram extends string = 'ADraQ2ENAbVoVZhvH5SPxWPsF2hH5YmFcgx61TafHuwu'> =
  | ({ instructionType: CounterInstruction.Increment } & ParsedIncrementInstruction<TProgram>)
  | ({ instructionType: CounterInstruction.Initialize } & ParsedInitializeInstruction<TProgram>)

export function parseCounterInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedCounterInstruction<TProgram> {
  const instructionType = identifyCounterInstruction(instruction)
  switch (instructionType) {
    case CounterInstruction.Increment: {
      assertIsInstructionWithAccounts(instruction)
      return { instructionType: CounterInstruction.Increment, ...parseIncrementInstruction(instruction) }
    }
    case CounterInstruction.Initialize: {
      assertIsInstructionWithAccounts(instruction)
      return { instructionType: CounterInstruction.Initialize, ...parseInitializeInstruction(instruction) }
    }
    default:
      throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE, {
        instructionType: instructionType as string,
        programName: 'counter',
      })
  }
}

export type CounterPlugin = {
  accounts: CounterPluginAccounts
  instructions: CounterPluginInstructions
  pdas: CounterPluginPdas
  identifyAccount: typeof identifyCounterAccount
  identifyInstruction: typeof identifyCounterInstruction
  parseInstruction: typeof parseCounterInstruction
}

export type CounterPluginAccounts = {
  counter: ReturnType<typeof getCounterCodec> & SelfFetchFunctions<CounterArgs, Counter>
}

export type CounterPluginInstructions = {
  increment: (input: IncrementAsyncInput) => ReturnType<typeof getIncrementInstructionAsync> & SelfPlanAndSendFunctions
  initialize: (
    input: MakeOptional<InitializeAsyncInput, 'payer'>,
  ) => ReturnType<typeof getInitializeInstructionAsync> & SelfPlanAndSendFunctions
}

export type CounterPluginPdas = { counter: typeof findCounterPda }

export type CounterPluginRequirements = ClientWithRpc<GetAccountInfoApi & GetMultipleAccountsApi> &
  ClientWithPayer &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending

export function counterProgram() {
  return <T extends CounterPluginRequirements>(client: T): ExtendedClient<T, { counter: CounterPlugin }> => {
    return extendClient(client, {
      counter: <CounterPlugin>{
        accounts: { counter: addSelfFetchFunctions(client, getCounterCodec()) },
        instructions: {
          increment: (input) => addSelfPlanAndSendFunctions(client, getIncrementInstructionAsync(input)),
          initialize: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeInstructionAsync({ ...input, payer: input.payer ?? client.payer }),
            ),
        },
        pdas: { counter: findCounterPda },
        identifyAccount: identifyCounterAccount,
        identifyInstruction: identifyCounterInstruction,
        parseInstruction: parseCounterInstruction,
      },
    })
  }
}

type MakeOptional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>
