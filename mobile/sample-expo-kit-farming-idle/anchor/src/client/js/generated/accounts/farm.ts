/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit'

export const FARM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([161, 156, 211, 253, 250, 64, 53, 250])

export function getFarmDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FARM_DISCRIMINATOR)
}

export type Farm = {
  discriminator: ReadonlyUint8Array
  /** The wallet that created the farm and holds the funds (signs via MWA). */
  owner: Address
  /** The local burner wallet that signs gameplay transactions without approval prompts. */
  player: Address
  bump: number
  createdAt: bigint
  lastHarvested: bigint
  harvestPoints: bigint
  /** Plots owned per crop, indexed like CROP_POINTS and CROP_COSTS. */
  crops: Array<number>
  highScore: bigint
}

export type FarmArgs = {
  /** The wallet that created the farm and holds the funds (signs via MWA). */
  owner: Address
  /** The local burner wallet that signs gameplay transactions without approval prompts. */
  player: Address
  bump: number
  createdAt: number | bigint
  lastHarvested: number | bigint
  harvestPoints: number | bigint
  /** Plots owned per crop, indexed like CROP_POINTS and CROP_COSTS. */
  crops: Array<number>
  highScore: number | bigint
}

/** Gets the encoder for {@link FarmArgs} account data. */
export function getFarmEncoder(): FixedSizeEncoder<FarmArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['owner', getAddressEncoder()],
      ['player', getAddressEncoder()],
      ['bump', getU8Encoder()],
      ['createdAt', getI64Encoder()],
      ['lastHarvested', getI64Encoder()],
      ['harvestPoints', getU64Encoder()],
      ['crops', getArrayEncoder(getU16Encoder(), { size: 8 })],
      ['highScore', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FARM_DISCRIMINATOR }),
  )
}

/** Gets the decoder for {@link Farm} account data. */
export function getFarmDecoder(): FixedSizeDecoder<Farm> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['owner', getAddressDecoder()],
    ['player', getAddressDecoder()],
    ['bump', getU8Decoder()],
    ['createdAt', getI64Decoder()],
    ['lastHarvested', getI64Decoder()],
    ['harvestPoints', getU64Decoder()],
    ['crops', getArrayDecoder(getU16Decoder(), { size: 8 })],
    ['highScore', getU64Decoder()],
  ])
}

/** Gets the codec for {@link Farm} account data. */
export function getFarmCodec(): FixedSizeCodec<FarmArgs, Farm> {
  return combineCodec(getFarmEncoder(), getFarmDecoder())
}

export function decodeFarm<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Farm, TAddress>
export function decodeFarm<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Farm, TAddress>
export function decodeFarm<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Farm, TAddress> | MaybeAccount<Farm, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getFarmDecoder())
}

export async function fetchFarm<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Farm, TAddress>> {
  const maybeAccount = await fetchMaybeFarm(rpc, address, config)
  assertAccountExists(maybeAccount)
  return maybeAccount
}

export async function fetchMaybeFarm<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Farm, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config)
  return decodeFarm(maybeAccount)
}

export async function fetchAllFarm(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Farm>[]> {
  const maybeAccounts = await fetchAllMaybeFarm(rpc, addresses, config)
  assertAccountsExist(maybeAccounts)
  return maybeAccounts
}

export async function fetchAllMaybeFarm(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Farm>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config)
  return maybeAccounts.map((maybeAccount) => decodeFarm(maybeAccount))
}

export function getFarmSize(): number {
  return 121
}
