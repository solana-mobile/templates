/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit'
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/kit/program-client-core'
import { findLeaderboardPda } from '../pdas'
import { FARMING_IDLE_PROGRAM_ADDRESS } from '../programs'

export const INITIALIZE_LEADERBOARD_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  47, 23, 34, 39, 46, 108, 91, 176,
])

export function getInitializeLeaderboardDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INITIALIZE_LEADERBOARD_DISCRIMINATOR)
}

export type InitializeLeaderboardInstruction<
  TProgram extends string = typeof FARMING_IDLE_PROGRAM_ADDRESS,
  TAccountLeaderboard extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountLeaderboard extends string ? WritableAccount<TAccountLeaderboard> : TAccountLeaderboard,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >

export type InitializeLeaderboardInstructionData = { discriminator: ReadonlyUint8Array }

export type InitializeLeaderboardInstructionDataArgs = {}

export function getInitializeLeaderboardInstructionDataEncoder(): FixedSizeEncoder<InitializeLeaderboardInstructionDataArgs> {
  return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({
    ...value,
    discriminator: INITIALIZE_LEADERBOARD_DISCRIMINATOR,
  }))
}

export function getInitializeLeaderboardInstructionDataDecoder(): FixedSizeDecoder<InitializeLeaderboardInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]])
}

export function getInitializeLeaderboardInstructionDataCodec(): FixedSizeCodec<
  InitializeLeaderboardInstructionDataArgs,
  InitializeLeaderboardInstructionData
> {
  return combineCodec(
    getInitializeLeaderboardInstructionDataEncoder(),
    getInitializeLeaderboardInstructionDataDecoder(),
  )
}

export type InitializeLeaderboardAsyncInput<
  TAccountLeaderboard extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  leaderboard?: Address<TAccountLeaderboard>
  payer: TransactionSigner<TAccountPayer>
  systemProgram?: Address<TAccountSystemProgram>
}

export async function getInitializeLeaderboardInstructionAsync<
  TAccountLeaderboard extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FARMING_IDLE_PROGRAM_ADDRESS,
>(
  input: InitializeLeaderboardAsyncInput<TAccountLeaderboard, TAccountPayer, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeLeaderboardInstruction<TProgramAddress, TAccountLeaderboard, TAccountPayer, TAccountSystemProgram>
> {
  // Program address.
  const programAddress = config?.programAddress ?? FARMING_IDLE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    leaderboard: { value: input.leaderboard ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Resolve default values.
  if (!accounts.leaderboard.value) {
    accounts.leaderboard.value = await findLeaderboardPda({ programAddress })
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('leaderboard', accounts.leaderboard),
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getInitializeLeaderboardInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeLeaderboardInstruction<TProgramAddress, TAccountLeaderboard, TAccountPayer, TAccountSystemProgram>)
}

export type InitializeLeaderboardInput<
  TAccountLeaderboard extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  leaderboard: Address<TAccountLeaderboard>
  payer: TransactionSigner<TAccountPayer>
  systemProgram?: Address<TAccountSystemProgram>
}

export function getInitializeLeaderboardInstruction<
  TAccountLeaderboard extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FARMING_IDLE_PROGRAM_ADDRESS,
>(
  input: InitializeLeaderboardInput<TAccountLeaderboard, TAccountPayer, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): InitializeLeaderboardInstruction<TProgramAddress, TAccountLeaderboard, TAccountPayer, TAccountSystemProgram> {
  // Program address.
  const programAddress = config?.programAddress ?? FARMING_IDLE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    leaderboard: { value: input.leaderboard ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('leaderboard', accounts.leaderboard),
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getInitializeLeaderboardInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeLeaderboardInstruction<TProgramAddress, TAccountLeaderboard, TAccountPayer, TAccountSystemProgram>)
}

export type ParsedInitializeLeaderboardInstruction<
  TProgram extends string = typeof FARMING_IDLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    leaderboard: TAccountMetas[0]
    payer: TAccountMetas[1]
    systemProgram: TAccountMetas[2]
  }
  data: InitializeLeaderboardInstructionData
}

export function parseInitializeLeaderboardInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeLeaderboardInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    })
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { leaderboard: getNextAccount(), payer: getNextAccount(), systemProgram: getNextAccount() },
    data: getInitializeLeaderboardInstructionDataDecoder().decode(instruction.data),
  }
}
