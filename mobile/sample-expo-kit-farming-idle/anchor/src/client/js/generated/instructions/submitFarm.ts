/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit'
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/kit/program-client-core'
import { findLeaderboardPda } from '../pdas'
import { FARMING_IDLE_PROGRAM_ADDRESS } from '../programs'

export const SUBMIT_FARM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([8, 195, 198, 253, 255, 190, 96, 102])

export function getSubmitFarmDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SUBMIT_FARM_DISCRIMINATOR)
}

export type SubmitFarmInstruction<
  TProgram extends string = typeof FARMING_IDLE_PROGRAM_ADDRESS,
  TAccountFarm extends string | AccountMeta<string> = string,
  TAccountLeaderboard extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFarm extends string ? WritableAccount<TAccountFarm> : TAccountFarm,
      TAccountLeaderboard extends string ? WritableAccount<TAccountLeaderboard> : TAccountLeaderboard,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountPlayer extends string
        ? ReadonlySignerAccount<TAccountPlayer> & AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      ...TRemainingAccounts,
    ]
  >

export type SubmitFarmInstructionData = { discriminator: ReadonlyUint8Array }

export type SubmitFarmInstructionDataArgs = {}

export function getSubmitFarmInstructionDataEncoder(): FixedSizeEncoder<SubmitFarmInstructionDataArgs> {
  return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({
    ...value,
    discriminator: SUBMIT_FARM_DISCRIMINATOR,
  }))
}

export function getSubmitFarmInstructionDataDecoder(): FixedSizeDecoder<SubmitFarmInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]])
}

export function getSubmitFarmInstructionDataCodec(): FixedSizeCodec<
  SubmitFarmInstructionDataArgs,
  SubmitFarmInstructionData
> {
  return combineCodec(getSubmitFarmInstructionDataEncoder(), getSubmitFarmInstructionDataDecoder())
}

export type SubmitFarmAsyncInput<
  TAccountFarm extends string = string,
  TAccountLeaderboard extends string = string,
  TAccountOwner extends string = string,
  TAccountPlayer extends string = string,
> = {
  farm: Address<TAccountFarm>
  leaderboard?: Address<TAccountLeaderboard>
  owner: TransactionSigner<TAccountOwner>
  player: TransactionSigner<TAccountPlayer>
}

export async function getSubmitFarmInstructionAsync<
  TAccountFarm extends string,
  TAccountLeaderboard extends string,
  TAccountOwner extends string,
  TAccountPlayer extends string,
  TProgramAddress extends Address = typeof FARMING_IDLE_PROGRAM_ADDRESS,
>(
  input: SubmitFarmAsyncInput<TAccountFarm, TAccountLeaderboard, TAccountOwner, TAccountPlayer>,
  config?: { programAddress?: TProgramAddress },
): Promise<SubmitFarmInstruction<TProgramAddress, TAccountFarm, TAccountLeaderboard, TAccountOwner, TAccountPlayer>> {
  // Program address.
  const programAddress = config?.programAddress ?? FARMING_IDLE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    farm: { value: input.farm ?? null, isWritable: true },
    leaderboard: { value: input.leaderboard ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    player: { value: input.player ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Resolve default values.
  if (!accounts.leaderboard.value) {
    accounts.leaderboard.value = await findLeaderboardPda({ programAddress })
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('farm', accounts.farm),
      getAccountMeta('leaderboard', accounts.leaderboard),
      getAccountMeta('owner', accounts.owner),
      getAccountMeta('player', accounts.player),
    ],
    data: getSubmitFarmInstructionDataEncoder().encode({}),
    programAddress,
  } as SubmitFarmInstruction<TProgramAddress, TAccountFarm, TAccountLeaderboard, TAccountOwner, TAccountPlayer>)
}

export type SubmitFarmInput<
  TAccountFarm extends string = string,
  TAccountLeaderboard extends string = string,
  TAccountOwner extends string = string,
  TAccountPlayer extends string = string,
> = {
  farm: Address<TAccountFarm>
  leaderboard: Address<TAccountLeaderboard>
  owner: TransactionSigner<TAccountOwner>
  player: TransactionSigner<TAccountPlayer>
}

export function getSubmitFarmInstruction<
  TAccountFarm extends string,
  TAccountLeaderboard extends string,
  TAccountOwner extends string,
  TAccountPlayer extends string,
  TProgramAddress extends Address = typeof FARMING_IDLE_PROGRAM_ADDRESS,
>(
  input: SubmitFarmInput<TAccountFarm, TAccountLeaderboard, TAccountOwner, TAccountPlayer>,
  config?: { programAddress?: TProgramAddress },
): SubmitFarmInstruction<TProgramAddress, TAccountFarm, TAccountLeaderboard, TAccountOwner, TAccountPlayer> {
  // Program address.
  const programAddress = config?.programAddress ?? FARMING_IDLE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    farm: { value: input.farm ?? null, isWritable: true },
    leaderboard: { value: input.leaderboard ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    player: { value: input.player ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('farm', accounts.farm),
      getAccountMeta('leaderboard', accounts.leaderboard),
      getAccountMeta('owner', accounts.owner),
      getAccountMeta('player', accounts.player),
    ],
    data: getSubmitFarmInstructionDataEncoder().encode({}),
    programAddress,
  } as SubmitFarmInstruction<TProgramAddress, TAccountFarm, TAccountLeaderboard, TAccountOwner, TAccountPlayer>)
}

export type ParsedSubmitFarmInstruction<
  TProgram extends string = typeof FARMING_IDLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    farm: TAccountMetas[0]
    leaderboard: TAccountMetas[1]
    owner: TAccountMetas[2]
    player: TAccountMetas[3]
  }
  data: SubmitFarmInstructionData
}

export function parseSubmitFarmInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedSubmitFarmInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    })
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      farm: getNextAccount(),
      leaderboard: getNextAccount(),
      owner: getNextAccount(),
      player: getNextAccount(),
    },
    data: getSubmitFarmInstructionDataDecoder().decode(instruction.data),
  }
}
