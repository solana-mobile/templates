/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit'
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/kit/program-client-core'
import { FARMING_IDLE_PROGRAM_ADDRESS } from '../programs'

export const UPGRADE_FARM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([110, 239, 193, 1, 165, 101, 54, 200])

export function getUpgradeFarmDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPGRADE_FARM_DISCRIMINATOR)
}

export type UpgradeFarmInstruction<
  TProgram extends string = typeof FARMING_IDLE_PROGRAM_ADDRESS,
  TAccountFarm extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFarm extends string ? WritableAccount<TAccountFarm> : TAccountFarm,
      TAccountPlayer extends string
        ? ReadonlySignerAccount<TAccountPlayer> & AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      ...TRemainingAccounts,
    ]
  >

export type UpgradeFarmInstructionData = { discriminator: ReadonlyUint8Array; cropIndex: number; amount: number }

export type UpgradeFarmInstructionDataArgs = { cropIndex: number; amount: number }

export function getUpgradeFarmInstructionDataEncoder(): FixedSizeEncoder<UpgradeFarmInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['cropIndex', getU8Encoder()],
      ['amount', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPGRADE_FARM_DISCRIMINATOR }),
  )
}

export function getUpgradeFarmInstructionDataDecoder(): FixedSizeDecoder<UpgradeFarmInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['cropIndex', getU8Decoder()],
    ['amount', getU8Decoder()],
  ])
}

export function getUpgradeFarmInstructionDataCodec(): FixedSizeCodec<
  UpgradeFarmInstructionDataArgs,
  UpgradeFarmInstructionData
> {
  return combineCodec(getUpgradeFarmInstructionDataEncoder(), getUpgradeFarmInstructionDataDecoder())
}

export type UpgradeFarmInput<TAccountFarm extends string = string, TAccountPlayer extends string = string> = {
  farm: Address<TAccountFarm>
  player: TransactionSigner<TAccountPlayer>
  cropIndex: UpgradeFarmInstructionDataArgs['cropIndex']
  amount: UpgradeFarmInstructionDataArgs['amount']
}

export function getUpgradeFarmInstruction<
  TAccountFarm extends string,
  TAccountPlayer extends string,
  TProgramAddress extends Address = typeof FARMING_IDLE_PROGRAM_ADDRESS,
>(
  input: UpgradeFarmInput<TAccountFarm, TAccountPlayer>,
  config?: { programAddress?: TProgramAddress },
): UpgradeFarmInstruction<TProgramAddress, TAccountFarm, TAccountPlayer> {
  // Program address.
  const programAddress = config?.programAddress ?? FARMING_IDLE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    farm: { value: input.farm ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [getAccountMeta('farm', accounts.farm), getAccountMeta('player', accounts.player)],
    data: getUpgradeFarmInstructionDataEncoder().encode(args as UpgradeFarmInstructionDataArgs),
    programAddress,
  } as UpgradeFarmInstruction<TProgramAddress, TAccountFarm, TAccountPlayer>)
}

export type ParsedUpgradeFarmInstruction<
  TProgram extends string = typeof FARMING_IDLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    farm: TAccountMetas[0]
    player: TAccountMetas[1]
  }
  data: UpgradeFarmInstructionData
}

export function parseUpgradeFarmInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedUpgradeFarmInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    })
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { farm: getNextAccount(), player: getNextAccount() },
    data: getUpgradeFarmInstructionDataDecoder().decode(instruction.data),
  }
}
