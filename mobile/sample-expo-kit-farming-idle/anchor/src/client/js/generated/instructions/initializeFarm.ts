/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit'
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core'
import { findFarmPda } from '../pdas'
import { FARMING_IDLE_PROGRAM_ADDRESS } from '../programs'

export const INITIALIZE_FARM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([252, 28, 185, 172, 244, 74, 117, 165])

export function getInitializeFarmDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INITIALIZE_FARM_DISCRIMINATOR)
}

export type InitializeFarmInstruction<
  TProgram extends string = typeof FARMING_IDLE_PROGRAM_ADDRESS,
  TAccountFarm extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFarm extends string ? WritableAccount<TAccountFarm> : TAccountFarm,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> & AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >

export type InitializeFarmInstructionData = { discriminator: ReadonlyUint8Array }

export type InitializeFarmInstructionDataArgs = {}

export function getInitializeFarmInstructionDataEncoder(): FixedSizeEncoder<InitializeFarmInstructionDataArgs> {
  return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({
    ...value,
    discriminator: INITIALIZE_FARM_DISCRIMINATOR,
  }))
}

export function getInitializeFarmInstructionDataDecoder(): FixedSizeDecoder<InitializeFarmInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]])
}

export function getInitializeFarmInstructionDataCodec(): FixedSizeCodec<
  InitializeFarmInstructionDataArgs,
  InitializeFarmInstructionData
> {
  return combineCodec(getInitializeFarmInstructionDataEncoder(), getInitializeFarmInstructionDataDecoder())
}

export type InitializeFarmAsyncInput<
  TAccountFarm extends string = string,
  TAccountOwner extends string = string,
  TAccountPlayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  farm?: Address<TAccountFarm>
  owner: TransactionSigner<TAccountOwner>
  player: TransactionSigner<TAccountPlayer>
  systemProgram?: Address<TAccountSystemProgram>
}

export async function getInitializeFarmInstructionAsync<
  TAccountFarm extends string,
  TAccountOwner extends string,
  TAccountPlayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FARMING_IDLE_PROGRAM_ADDRESS,
>(
  input: InitializeFarmAsyncInput<TAccountFarm, TAccountOwner, TAccountPlayer, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeFarmInstruction<TProgramAddress, TAccountFarm, TAccountOwner, TAccountPlayer, TAccountSystemProgram>
> {
  // Program address.
  const programAddress = config?.programAddress ?? FARMING_IDLE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    farm: { value: input.farm ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Resolve default values.
  if (!accounts.farm.value) {
    accounts.farm.value = await findFarmPda(
      {
        player: getAddressFromResolvedInstructionAccount('player', accounts.player.value),
        owner: getAddressFromResolvedInstructionAccount('owner', accounts.owner.value),
      },
      { programAddress },
    )
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('farm', accounts.farm),
      getAccountMeta('owner', accounts.owner),
      getAccountMeta('player', accounts.player),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getInitializeFarmInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeFarmInstruction<TProgramAddress, TAccountFarm, TAccountOwner, TAccountPlayer, TAccountSystemProgram>)
}

export type InitializeFarmInput<
  TAccountFarm extends string = string,
  TAccountOwner extends string = string,
  TAccountPlayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  farm: Address<TAccountFarm>
  owner: TransactionSigner<TAccountOwner>
  player: TransactionSigner<TAccountPlayer>
  systemProgram?: Address<TAccountSystemProgram>
}

export function getInitializeFarmInstruction<
  TAccountFarm extends string,
  TAccountOwner extends string,
  TAccountPlayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FARMING_IDLE_PROGRAM_ADDRESS,
>(
  input: InitializeFarmInput<TAccountFarm, TAccountOwner, TAccountPlayer, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): InitializeFarmInstruction<TProgramAddress, TAccountFarm, TAccountOwner, TAccountPlayer, TAccountSystemProgram> {
  // Program address.
  const programAddress = config?.programAddress ?? FARMING_IDLE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    farm: { value: input.farm ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('farm', accounts.farm),
      getAccountMeta('owner', accounts.owner),
      getAccountMeta('player', accounts.player),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getInitializeFarmInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeFarmInstruction<TProgramAddress, TAccountFarm, TAccountOwner, TAccountPlayer, TAccountSystemProgram>)
}

export type ParsedInitializeFarmInstruction<
  TProgram extends string = typeof FARMING_IDLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    farm: TAccountMetas[0]
    owner: TAccountMetas[1]
    player: TAccountMetas[2]
    systemProgram: TAccountMetas[3]
  }
  data: InitializeFarmInstructionData
}

export function parseInitializeFarmInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeFarmInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    })
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      farm: getNextAccount(),
      owner: getNextAccount(),
      player: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeFarmInstructionDataDecoder().decode(instruction.data),
  }
}
