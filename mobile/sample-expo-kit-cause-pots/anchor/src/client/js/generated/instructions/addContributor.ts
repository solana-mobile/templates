/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit'
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/kit/program-client-core'
import { CAUSE_POTS_PROGRAM_ADDRESS } from '../programs'

export const ADD_CONTRIBUTOR_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([125, 221, 162, 182, 191, 26, 206, 219])

export function getAddContributorDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_CONTRIBUTOR_DISCRIMINATOR)
}

export type AddContributorInstruction<
  TProgram extends string = typeof CAUSE_POTS_PROGRAM_ADDRESS,
  TAccountPot extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPot extends string ? WritableAccount<TAccountPot> : TAccountPot,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >

export type AddContributorInstructionData = { discriminator: ReadonlyUint8Array; newContributor: Address }

export type AddContributorInstructionDataArgs = { newContributor: Address }

export function getAddContributorInstructionDataEncoder(): FixedSizeEncoder<AddContributorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newContributor', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_CONTRIBUTOR_DISCRIMINATOR }),
  )
}

export function getAddContributorInstructionDataDecoder(): FixedSizeDecoder<AddContributorInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newContributor', getAddressDecoder()],
  ])
}

export function getAddContributorInstructionDataCodec(): FixedSizeCodec<
  AddContributorInstructionDataArgs,
  AddContributorInstructionData
> {
  return combineCodec(getAddContributorInstructionDataEncoder(), getAddContributorInstructionDataDecoder())
}

export type AddContributorInput<TAccountPot extends string = string, TAccountAuthority extends string = string> = {
  pot: Address<TAccountPot>
  authority: TransactionSigner<TAccountAuthority>
  newContributor: AddContributorInstructionDataArgs['newContributor']
}

export function getAddContributorInstruction<
  TAccountPot extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof CAUSE_POTS_PROGRAM_ADDRESS,
>(
  input: AddContributorInput<TAccountPot, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): AddContributorInstruction<TProgramAddress, TAccountPot, TAccountAuthority> {
  // Program address.
  const programAddress = config?.programAddress ?? CAUSE_POTS_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    pot: { value: input.pot ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [getAccountMeta('pot', accounts.pot), getAccountMeta('authority', accounts.authority)],
    data: getAddContributorInstructionDataEncoder().encode(args as AddContributorInstructionDataArgs),
    programAddress,
  } as AddContributorInstruction<TProgramAddress, TAccountPot, TAccountAuthority>)
}

export type ParsedAddContributorInstruction<
  TProgram extends string = typeof CAUSE_POTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    pot: TAccountMetas[0]
    authority: TAccountMetas[1]
  }
  data: AddContributorInstructionData
}

export function parseAddContributorInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedAddContributorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    })
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { pot: getNextAccount(), authority: getNextAccount() },
    data: getAddContributorInstructionDataDecoder().decode(instruction.data),
  }
}
