/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit'
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core'
import { findPotPda, findVaultPda } from '../pdas'
import { CAUSE_POTS_PROGRAM_ADDRESS } from '../programs'
import {
  getCurrencyDecoder,
  getCurrencyEncoder,
  getPotCategoryDecoder,
  getPotCategoryEncoder,
  type Currency,
  type CurrencyArgs,
  type PotCategory,
  type PotCategoryArgs,
} from '../types'

export const CREATE_POT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([232, 45, 123, 181, 204, 121, 131, 9])

export function getCreatePotDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_POT_DISCRIMINATOR)
}

export type CreatePotInstruction<
  TProgram extends string = typeof CAUSE_POTS_PROGRAM_ADDRESS,
  TAccountPot extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPot extends string ? WritableAccount<TAccountPot> : TAccountPot,
      TAccountVault extends string ? ReadonlyAccount<TAccountVault> : TAccountVault,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >

export type CreatePotInstructionData = {
  discriminator: ReadonlyUint8Array
  name: string
  description: string
  category: PotCategory
  currency: Currency
  targetAmount: bigint
  unlockDays: bigint
  signersRequired: number
}

export type CreatePotInstructionDataArgs = {
  name: string
  description: string
  category: PotCategoryArgs
  currency: CurrencyArgs
  targetAmount: number | bigint
  unlockDays: number | bigint
  signersRequired: number
}

export function getCreatePotInstructionDataEncoder(): Encoder<CreatePotInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['description', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['category', getPotCategoryEncoder()],
      ['currency', getCurrencyEncoder()],
      ['targetAmount', getU64Encoder()],
      ['unlockDays', getI64Encoder()],
      ['signersRequired', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_POT_DISCRIMINATOR }),
  )
}

export function getCreatePotInstructionDataDecoder(): Decoder<CreatePotInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['description', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['category', getPotCategoryDecoder()],
    ['currency', getCurrencyDecoder()],
    ['targetAmount', getU64Decoder()],
    ['unlockDays', getI64Decoder()],
    ['signersRequired', getU8Decoder()],
  ])
}

export function getCreatePotInstructionDataCodec(): Codec<CreatePotInstructionDataArgs, CreatePotInstructionData> {
  return combineCodec(getCreatePotInstructionDataEncoder(), getCreatePotInstructionDataDecoder())
}

export type CreatePotAsyncInput<
  TAccountPot extends string = string,
  TAccountVault extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pot?: Address<TAccountPot>
  vault?: Address<TAccountVault>
  authority: TransactionSigner<TAccountAuthority>
  systemProgram?: Address<TAccountSystemProgram>
  name: CreatePotInstructionDataArgs['name']
  description: CreatePotInstructionDataArgs['description']
  category: CreatePotInstructionDataArgs['category']
  currency: CreatePotInstructionDataArgs['currency']
  targetAmount: CreatePotInstructionDataArgs['targetAmount']
  unlockDays: CreatePotInstructionDataArgs['unlockDays']
  signersRequired: CreatePotInstructionDataArgs['signersRequired']
}

export async function getCreatePotInstructionAsync<
  TAccountPot extends string,
  TAccountVault extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CAUSE_POTS_PROGRAM_ADDRESS,
>(
  input: CreatePotAsyncInput<TAccountPot, TAccountVault, TAccountAuthority, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreatePotInstruction<TProgramAddress, TAccountPot, TAccountVault, TAccountAuthority, TAccountSystemProgram>
> {
  // Program address.
  const programAddress = config?.programAddress ?? CAUSE_POTS_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    pot: { value: input.pot ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Original args.
  const args = { ...input }

  // Resolve default values.
  if (!accounts.pot.value) {
    accounts.pot.value = await findPotPda(
      {
        authority: getAddressFromResolvedInstructionAccount('authority', accounts.authority.value),
        name: getNonNullResolvedInstructionInput('name', args.name),
      },
      { programAddress },
    )
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda(
      { pot: getAddressFromResolvedInstructionAccount('pot', accounts.pot.value) },
      { programAddress },
    )
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('pot', accounts.pot),
      getAccountMeta('vault', accounts.vault),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getCreatePotInstructionDataEncoder().encode(args as CreatePotInstructionDataArgs),
    programAddress,
  } as CreatePotInstruction<TProgramAddress, TAccountPot, TAccountVault, TAccountAuthority, TAccountSystemProgram>)
}

export type CreatePotInput<
  TAccountPot extends string = string,
  TAccountVault extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pot: Address<TAccountPot>
  vault: Address<TAccountVault>
  authority: TransactionSigner<TAccountAuthority>
  systemProgram?: Address<TAccountSystemProgram>
  name: CreatePotInstructionDataArgs['name']
  description: CreatePotInstructionDataArgs['description']
  category: CreatePotInstructionDataArgs['category']
  currency: CreatePotInstructionDataArgs['currency']
  targetAmount: CreatePotInstructionDataArgs['targetAmount']
  unlockDays: CreatePotInstructionDataArgs['unlockDays']
  signersRequired: CreatePotInstructionDataArgs['signersRequired']
}

export function getCreatePotInstruction<
  TAccountPot extends string,
  TAccountVault extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CAUSE_POTS_PROGRAM_ADDRESS,
>(
  input: CreatePotInput<TAccountPot, TAccountVault, TAccountAuthority, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): CreatePotInstruction<TProgramAddress, TAccountPot, TAccountVault, TAccountAuthority, TAccountSystemProgram> {
  // Program address.
  const programAddress = config?.programAddress ?? CAUSE_POTS_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    pot: { value: input.pot ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Original args.
  const args = { ...input }

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('pot', accounts.pot),
      getAccountMeta('vault', accounts.vault),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getCreatePotInstructionDataEncoder().encode(args as CreatePotInstructionDataArgs),
    programAddress,
  } as CreatePotInstruction<TProgramAddress, TAccountPot, TAccountVault, TAccountAuthority, TAccountSystemProgram>)
}

export type ParsedCreatePotInstruction<
  TProgram extends string = typeof CAUSE_POTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    pot: TAccountMetas[0]
    vault: TAccountMetas[1]
    authority: TAccountMetas[2]
    systemProgram: TAccountMetas[3]
  }
  data: CreatePotInstructionData
}

export function parseCreatePotInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatePotInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    })
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pot: getNextAccount(),
      vault: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreatePotInstructionDataDecoder().decode(instruction.data),
  }
}
