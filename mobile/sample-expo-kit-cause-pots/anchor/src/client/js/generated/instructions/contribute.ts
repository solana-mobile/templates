/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit'
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core'
import { findContributorAccountPda, findVaultPda } from '../pdas'
import { CAUSE_POTS_PROGRAM_ADDRESS } from '../programs'

export const CONTRIBUTE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([82, 33, 68, 131, 32, 0, 205, 95])

export function getContributeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CONTRIBUTE_DISCRIMINATOR)
}

export type ContributeInstruction<
  TProgram extends string = typeof CAUSE_POTS_PROGRAM_ADDRESS,
  TAccountPot extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountContributorAccount extends string | AccountMeta<string> = string,
  TAccountContributor extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPot extends string ? WritableAccount<TAccountPot> : TAccountPot,
      TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault,
      TAccountContributorAccount extends string
        ? WritableAccount<TAccountContributorAccount>
        : TAccountContributorAccount,
      TAccountContributor extends string
        ? WritableSignerAccount<TAccountContributor> & AccountSignerMeta<TAccountContributor>
        : TAccountContributor,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >

export type ContributeInstructionData = { discriminator: ReadonlyUint8Array; amount: bigint }

export type ContributeInstructionDataArgs = { amount: number | bigint }

export function getContributeInstructionDataEncoder(): FixedSizeEncoder<ContributeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CONTRIBUTE_DISCRIMINATOR }),
  )
}

export function getContributeInstructionDataDecoder(): FixedSizeDecoder<ContributeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ])
}

export function getContributeInstructionDataCodec(): FixedSizeCodec<
  ContributeInstructionDataArgs,
  ContributeInstructionData
> {
  return combineCodec(getContributeInstructionDataEncoder(), getContributeInstructionDataDecoder())
}

export type ContributeAsyncInput<
  TAccountPot extends string = string,
  TAccountVault extends string = string,
  TAccountContributorAccount extends string = string,
  TAccountContributor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pot: Address<TAccountPot>
  vault?: Address<TAccountVault>
  contributorAccount?: Address<TAccountContributorAccount>
  contributor: TransactionSigner<TAccountContributor>
  systemProgram?: Address<TAccountSystemProgram>
  amount: ContributeInstructionDataArgs['amount']
}

export async function getContributeInstructionAsync<
  TAccountPot extends string,
  TAccountVault extends string,
  TAccountContributorAccount extends string,
  TAccountContributor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CAUSE_POTS_PROGRAM_ADDRESS,
>(
  input: ContributeAsyncInput<
    TAccountPot,
    TAccountVault,
    TAccountContributorAccount,
    TAccountContributor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ContributeInstruction<
    TProgramAddress,
    TAccountPot,
    TAccountVault,
    TAccountContributorAccount,
    TAccountContributor,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CAUSE_POTS_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    pot: { value: input.pot ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    contributorAccount: { value: input.contributorAccount ?? null, isWritable: true },
    contributor: { value: input.contributor ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Original args.
  const args = { ...input }

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda(
      { pot: getAddressFromResolvedInstructionAccount('pot', accounts.pot.value) },
      { programAddress },
    )
  }
  if (!accounts.contributorAccount.value) {
    accounts.contributorAccount.value = await findContributorAccountPda(
      {
        pot: getAddressFromResolvedInstructionAccount('pot', accounts.pot.value),
        contributor: getAddressFromResolvedInstructionAccount('contributor', accounts.contributor.value),
      },
      { programAddress },
    )
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('pot', accounts.pot),
      getAccountMeta('vault', accounts.vault),
      getAccountMeta('contributorAccount', accounts.contributorAccount),
      getAccountMeta('contributor', accounts.contributor),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getContributeInstructionDataEncoder().encode(args as ContributeInstructionDataArgs),
    programAddress,
  } as ContributeInstruction<
    TProgramAddress,
    TAccountPot,
    TAccountVault,
    TAccountContributorAccount,
    TAccountContributor,
    TAccountSystemProgram
  >)
}

export type ContributeInput<
  TAccountPot extends string = string,
  TAccountVault extends string = string,
  TAccountContributorAccount extends string = string,
  TAccountContributor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pot: Address<TAccountPot>
  vault: Address<TAccountVault>
  contributorAccount: Address<TAccountContributorAccount>
  contributor: TransactionSigner<TAccountContributor>
  systemProgram?: Address<TAccountSystemProgram>
  amount: ContributeInstructionDataArgs['amount']
}

export function getContributeInstruction<
  TAccountPot extends string,
  TAccountVault extends string,
  TAccountContributorAccount extends string,
  TAccountContributor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CAUSE_POTS_PROGRAM_ADDRESS,
>(
  input: ContributeInput<
    TAccountPot,
    TAccountVault,
    TAccountContributorAccount,
    TAccountContributor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ContributeInstruction<
  TProgramAddress,
  TAccountPot,
  TAccountVault,
  TAccountContributorAccount,
  TAccountContributor,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CAUSE_POTS_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    pot: { value: input.pot ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    contributorAccount: { value: input.contributorAccount ?? null, isWritable: true },
    contributor: { value: input.contributor ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  // Original args.
  const args = { ...input }

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('pot', accounts.pot),
      getAccountMeta('vault', accounts.vault),
      getAccountMeta('contributorAccount', accounts.contributorAccount),
      getAccountMeta('contributor', accounts.contributor),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getContributeInstructionDataEncoder().encode(args as ContributeInstructionDataArgs),
    programAddress,
  } as ContributeInstruction<
    TProgramAddress,
    TAccountPot,
    TAccountVault,
    TAccountContributorAccount,
    TAccountContributor,
    TAccountSystemProgram
  >)
}

export type ParsedContributeInstruction<
  TProgram extends string = typeof CAUSE_POTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    pot: TAccountMetas[0]
    vault: TAccountMetas[1]
    contributorAccount: TAccountMetas[2]
    contributor: TAccountMetas[3]
    systemProgram: TAccountMetas[4]
  }
  data: ContributeInstructionData
}

export function parseContributeInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedContributeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    })
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pot: getNextAccount(),
      vault: getNextAccount(),
      contributorAccount: getNextAccount(),
      contributor: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getContributeInstructionDataDecoder().decode(instruction.data),
  }
}
