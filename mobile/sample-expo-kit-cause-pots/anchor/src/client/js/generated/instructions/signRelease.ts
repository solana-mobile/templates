/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit'
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/kit/program-client-core'
import { CAUSE_POTS_PROGRAM_ADDRESS } from '../programs'

export const SIGN_RELEASE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([111, 211, 85, 96, 119, 69, 197, 105])

export function getSignReleaseDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SIGN_RELEASE_DISCRIMINATOR)
}

export type SignReleaseInstruction<
  TProgram extends string = typeof CAUSE_POTS_PROGRAM_ADDRESS,
  TAccountPot extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPot extends string ? WritableAccount<TAccountPot> : TAccountPot,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> & AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >

export type SignReleaseInstructionData = { discriminator: ReadonlyUint8Array }

export type SignReleaseInstructionDataArgs = {}

export function getSignReleaseInstructionDataEncoder(): FixedSizeEncoder<SignReleaseInstructionDataArgs> {
  return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({
    ...value,
    discriminator: SIGN_RELEASE_DISCRIMINATOR,
  }))
}

export function getSignReleaseInstructionDataDecoder(): FixedSizeDecoder<SignReleaseInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]])
}

export function getSignReleaseInstructionDataCodec(): FixedSizeCodec<
  SignReleaseInstructionDataArgs,
  SignReleaseInstructionData
> {
  return combineCodec(getSignReleaseInstructionDataEncoder(), getSignReleaseInstructionDataDecoder())
}

export type SignReleaseInput<TAccountPot extends string = string, TAccountSigner extends string = string> = {
  pot: Address<TAccountPot>
  signer: TransactionSigner<TAccountSigner>
}

export function getSignReleaseInstruction<
  TAccountPot extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof CAUSE_POTS_PROGRAM_ADDRESS,
>(
  input: SignReleaseInput<TAccountPot, TAccountSigner>,
  config?: { programAddress?: TProgramAddress },
): SignReleaseInstruction<TProgramAddress, TAccountPot, TAccountSigner> {
  // Program address.
  const programAddress = config?.programAddress ?? CAUSE_POTS_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    pot: { value: input.pot ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [getAccountMeta('pot', accounts.pot), getAccountMeta('signer', accounts.signer)],
    data: getSignReleaseInstructionDataEncoder().encode({}),
    programAddress,
  } as SignReleaseInstruction<TProgramAddress, TAccountPot, TAccountSigner>)
}

export type ParsedSignReleaseInstruction<
  TProgram extends string = typeof CAUSE_POTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    pot: TAccountMetas[0]
    signer: TAccountMetas[1]
  }
  data: SignReleaseInstructionData
}

export function parseSignReleaseInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedSignReleaseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 2,
    })
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { pot: getNextAccount(), signer: getNextAccount() },
    data: getSignReleaseInstructionDataDecoder().decode(instruction.data),
  }
}
