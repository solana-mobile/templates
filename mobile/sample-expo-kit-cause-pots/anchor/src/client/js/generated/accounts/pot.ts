/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from '@solana/kit'
import {
  getCurrencyDecoder,
  getCurrencyEncoder,
  getPotCategoryDecoder,
  getPotCategoryEncoder,
  type Currency,
  type CurrencyArgs,
  type PotCategory,
  type PotCategoryArgs,
} from '../types'

export const POT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([238, 118, 60, 175, 178, 191, 59, 58])

export function getPotDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(POT_DISCRIMINATOR)
}

export type Pot = {
  discriminator: ReadonlyUint8Array
  /** Creator of the pot; the only account that can add contributors and release funds. */
  authority: Address
  /** Vault PDA that holds the pooled SOL. */
  vault: Address
  name: string
  description: string
  category: PotCategory
  currency: Currency
  /** Savings target in lamports. */
  targetAmount: bigint
  totalContributed: bigint
  /** Release becomes possible once the cluster clock passes this timestamp. */
  unlockTimestamp: bigint
  /** How many contributor signatures a release requires. */
  signersRequired: number
  /**
   * Contributors that have approved the release. Each contributor can sign
   * once, so this never grows past the contributor list.
   */
  signatures: Array<Address>
  contributors: Array<Address>
  isReleased: boolean
  releasedAt: Option<bigint>
  recipient: Option<Address>
  createdAt: bigint
  bump: number
  vaultBump: number
}

export type PotArgs = {
  /** Creator of the pot; the only account that can add contributors and release funds. */
  authority: Address
  /** Vault PDA that holds the pooled SOL. */
  vault: Address
  name: string
  description: string
  category: PotCategoryArgs
  currency: CurrencyArgs
  /** Savings target in lamports. */
  targetAmount: number | bigint
  totalContributed: number | bigint
  /** Release becomes possible once the cluster clock passes this timestamp. */
  unlockTimestamp: number | bigint
  /** How many contributor signatures a release requires. */
  signersRequired: number
  /**
   * Contributors that have approved the release. Each contributor can sign
   * once, so this never grows past the contributor list.
   */
  signatures: Array<Address>
  contributors: Array<Address>
  isReleased: boolean
  releasedAt: OptionOrNullable<number | bigint>
  recipient: OptionOrNullable<Address>
  createdAt: number | bigint
  bump: number
  vaultBump: number
}

/** Gets the encoder for {@link PotArgs} account data. */
export function getPotEncoder(): Encoder<PotArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['authority', getAddressEncoder()],
      ['vault', getAddressEncoder()],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['description', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['category', getPotCategoryEncoder()],
      ['currency', getCurrencyEncoder()],
      ['targetAmount', getU64Encoder()],
      ['totalContributed', getU64Encoder()],
      ['unlockTimestamp', getI64Encoder()],
      ['signersRequired', getU8Encoder()],
      ['signatures', getArrayEncoder(getAddressEncoder())],
      ['contributors', getArrayEncoder(getAddressEncoder())],
      ['isReleased', getBooleanEncoder()],
      ['releasedAt', getOptionEncoder(getI64Encoder())],
      ['recipient', getOptionEncoder(getAddressEncoder())],
      ['createdAt', getI64Encoder()],
      ['bump', getU8Encoder()],
      ['vaultBump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: POT_DISCRIMINATOR }),
  )
}

/** Gets the decoder for {@link Pot} account data. */
export function getPotDecoder(): Decoder<Pot> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['authority', getAddressDecoder()],
    ['vault', getAddressDecoder()],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['description', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['category', getPotCategoryDecoder()],
    ['currency', getCurrencyDecoder()],
    ['targetAmount', getU64Decoder()],
    ['totalContributed', getU64Decoder()],
    ['unlockTimestamp', getI64Decoder()],
    ['signersRequired', getU8Decoder()],
    ['signatures', getArrayDecoder(getAddressDecoder())],
    ['contributors', getArrayDecoder(getAddressDecoder())],
    ['isReleased', getBooleanDecoder()],
    ['releasedAt', getOptionDecoder(getI64Decoder())],
    ['recipient', getOptionDecoder(getAddressDecoder())],
    ['createdAt', getI64Decoder()],
    ['bump', getU8Decoder()],
    ['vaultBump', getU8Decoder()],
  ])
}

/** Gets the codec for {@link Pot} account data. */
export function getPotCodec(): Codec<PotArgs, Pot> {
  return combineCodec(getPotEncoder(), getPotDecoder())
}

export function decodePot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Pot, TAddress>
export function decodePot<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Pot, TAddress>
export function decodePot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Pot, TAddress> | MaybeAccount<Pot, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getPotDecoder())
}

export async function fetchPot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Pot, TAddress>> {
  const maybeAccount = await fetchMaybePot(rpc, address, config)
  assertAccountExists(maybeAccount)
  return maybeAccount
}

export async function fetchMaybePot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Pot, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config)
  return decodePot(maybeAccount)
}

export async function fetchAllPot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Pot>[]> {
  const maybeAccounts = await fetchAllMaybePot(rpc, addresses, config)
  assertAccountsExist(maybeAccounts)
  return maybeAccounts
}

export async function fetchAllMaybePot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Pot>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config)
  return maybeAccounts.map((maybeAccount) => decodePot(maybeAccount))
}
